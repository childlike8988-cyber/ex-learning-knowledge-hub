"use client";
import { useState } from "react";
import { exerciseLevels } from "@/data/lifeManagement";
export function ExerciseLevelSelector() { const [selected, setSelected] = useState(0); const level = exerciseLevels[selected]; return <section id="exercise" className="scroll-mt-24 py-12"><p className="eyebrow">Move with care</p><h2 className="mt-3 text-3xl font-semibold text-white">三級運動需求</h2><div role="tablist" aria-label="選擇運動等級" className="mt-6 flex gap-2 overflow-x-auto pb-2">{exerciseLevels.map((item, index) => <button type="button" role="tab" id={`${item.id}-tab`} aria-selected={selected === index} aria-controls={`${item.id}-panel`} key={item.id} onClick={() => setSelected(index)} className={`min-h-11 shrink-0 rounded-full border px-4 text-sm font-semibold transition focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-cyan-200 ${selected === index ? "border-cyan-300 bg-cyan-300 text-slate-950" : "border-white/15 text-slate-300 hover:border-cyan-300/50"}`}>{item.name}</button>)}</div><div role="tabpanel" id={`${level.id}-panel`} aria-labelledby={`${level.id}-tab`} className="glass life-fade mt-4 rounded-2xl p-6"><p className="text-sm font-semibold text-amber-200">{level.positioning}</p><ul className="mt-4 grid gap-3 sm:grid-cols-2">{level.items.map((item) => <li className="rounded-xl border border-white/10 bg-white/5 px-4 py-3 text-sm text-slate-200" key={item}>{item}</li>)}</ul></div><p className="mt-5 rounded-xl border border-amber-200/20 bg-amber-200/5 p-4 text-sm leading-7 text-amber-50">運動量請依個人年齡、體能、關節狀況與當日身體狀態彈性增減。動作品質優先於次數，循序增加，勿勉強超越自身現況。</p></section>; }
