import Image from "next/image";
import { lifeAssetPaths, weeklyExercises } from "@/data/lifeManagement";
import { publicAssetPath } from "@/lib/paths";
export function WeeklyExerciseOptions() { const image = publicAssetPath(lifeAssetPaths.weeklyExercise); return <section className="py-12"><p className="eyebrow">Add one more</p><h2 className="mt-3 text-3xl font-semibold text-white">每週追加運動</h2><p className="mt-3 max-w-3xl leading-7 text-slate-300">中等強度有氧每週累積 150 分鐘即可帶來健康效益；搭配每週至少 2 天肌力訓練，能更完整地支援體態、心肺與健康習慣。</p><a href={image} target="_blank" rel="noopener noreferrer" aria-label="在新分頁查看完整 YUNI 每週追加運動示意圖" className="glass mt-6 block overflow-hidden rounded-2xl transition hover:border-cyan-300/50 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-cyan-200"><Image src={image} alt="YUNI 游泳、爬山、慢跑、單車與重量訓練的每週追加運動示意圖" width={1600} height={900} unoptimized className="h-auto w-full object-contain" /><span className="block px-4 py-3 text-xs text-slate-400">點擊可在新分頁放大查看完整運動示意圖。</span></a><div className="mt-5 flex snap-x gap-3 overflow-x-auto pb-3">{weeklyExercises.map((item) => <article className="glass min-w-52 snap-start rounded-2xl p-5" key={item.title}><h3 className="font-semibold text-white">{item.title}</h3><p className="mt-3 text-sm leading-6 text-slate-300">{item.description}</p></article>)}</div></section>; }
