import Image from "next/image";
import { breakfastSections, lifeAssetPaths } from "@/data/lifeManagement";
import { publicAssetPath } from "@/lib/paths";
export function BreakfastManagement() { const image = publicAssetPath(lifeAssetPaths.breakfast); return <section id="breakfast" className="scroll-mt-24 py-12"><p className="eyebrow text-amber-100">Breakfast management</p><h2 className="mt-3 text-3xl font-semibold text-white">從一杯早餐開始</h2><p className="mt-3 max-w-3xl leading-7 text-slate-300">早餐的重點，是建立方便、能持續且符合個人需求的飲食方式。</p><div className="mt-6 grid gap-5 lg:grid-cols-[1fr_1.1fr]"><a href={image} target="_blank" rel="noopener noreferrer" aria-label="在新分頁查看一杯早餐計畫主視覺" className="glass block overflow-hidden rounded-2xl transition hover:border-amber-200/50 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-amber-100"><Image src={image} alt="一杯早餐計畫的溫暖早餐生活場景主視覺" width={1200} height={900} unoptimized className="h-auto w-full object-contain" /><span className="block px-4 py-3 text-xs text-slate-400">一杯早餐計畫主視覺：以日常飲食與生活節奏為核心的情境示意。</span></a><div className="grid gap-3">{breakfastSections.map((section) => <article className="glass rounded-2xl p-5" key={section.title}><h3 className="font-semibold text-white">{section.title}</h3><ul className="mt-3 flex flex-wrap gap-2">{section.items.map((item) => <li className="rounded-full border border-white/10 px-3 py-1.5 text-xs text-slate-300" key={item}>{item}</li>)}</ul></article>)}</div></div></section>; }
