import Image from "next/image";
import { homeMovements, lifeAssetPaths } from "@/data/lifeManagement";
import { publicAssetPath } from "@/lib/paths";
export function HomeExerciseLibrary() { const image = publicAssetPath(lifeAssetPaths.homeExercise); return <section className="py-12"><p className="eyebrow">Home movement library</p><h2 className="mt-3 text-3xl font-semibold text-white">居家動作分解</h2><a href={image} target="_blank" rel="noopener noreferrer" aria-label="在新分頁查看完整 YUNI 居家動作分解圖" className="glass mt-6 block overflow-hidden rounded-2xl transition hover:border-cyan-300/50 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-cyan-200"><Image src={image} alt="YUNI 居家四動作分解圖，包含跨下提膝、側向提膝、慢速深蹲與墊腳回春術的起始及動作示意" width={1600} height={900} unoptimized className="h-auto w-full object-contain" /><span className="block px-4 py-3 text-xs text-slate-400">點擊可在新分頁放大查看完整動作標籤。</span></a><div className="mt-5 grid gap-4 sm:grid-cols-2">{homeMovements.map((movement) => <article className="glass rounded-2xl p-5" key={movement.title}><h3 className="text-lg font-semibold text-white">{movement.title}</h3><p className="mt-3 text-sm leading-6 text-slate-200">{movement.cue}</p><p className="mt-4 text-xs text-cyan-100">建議次數：50 → 100 下</p><p className="mt-2 text-xs leading-5 text-slate-400">常見錯誤：{movement.mistake}</p></article>)}</div></section>; }
