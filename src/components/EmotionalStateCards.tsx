"use client";
import { useState } from "react";
import { emotionalPractices } from "@/data/lifeManagement";
export function EmotionalStateCards() { const [done, setDone] = useState<string[]>([]); const toggle = (title: string) => setDone((items) => items.includes(title) ? items.filter((item) => item !== title) : [...items, title]); return <section id="emotion" className="scroll-mt-24 py-12"><p className="eyebrow">Steady state</p><h2 className="mt-3 text-3xl font-semibold text-white">情緒狀態</h2><p className="mt-3 text-lg text-cyan-100">讓自己處於自信、快樂與穩定的狀態。</p><p className="mt-3 max-w-3xl leading-7 text-slate-300">生活不需要每天維持完美狀態。低潮時可以降低任務、休息或尋求支持。</p><div className="mt-6 grid gap-4 sm:grid-cols-2 lg:grid-cols-4">{emotionalPractices.map((item) => { const active = done.includes(item.title); return <button type="button" aria-pressed={active} onClick={() => toggle(item.title)} className={`glass min-h-44 rounded-2xl p-5 text-left transition hover:-translate-y-1 focus-visible:outline focus-visible:outline-2 focus-visible:outline-offset-4 focus-visible:outline-cyan-200 ${active ? "border-cyan-300/70 bg-cyan-300/10" : ""}`} key={item.title}><span className="text-xs font-bold tracking-widest text-cyan-300">{active ? "今天已完成" : "輕量練習"}</span><h3 className="mt-4 text-lg font-semibold text-white">{item.title}</h3><p className="mt-2 text-sm leading-6 text-slate-300">{item.description}</p></button>; })}</div><p className="mt-4 text-xs text-slate-400">此頁面的完成狀態僅保留於目前瀏覽階段，重新整理後會重設。</p></section>; }
