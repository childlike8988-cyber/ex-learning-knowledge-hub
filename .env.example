# Public Supabase browser configuration for Market Radar Phase 2F-1B.
# Use either the modern publishable key or the legacy anon key, never both.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Never add service_role keys, database passwords, OAuth client secrets,
# payment secrets or backend-only tokens to this file or any NEXT_PUBLIC_ key.
